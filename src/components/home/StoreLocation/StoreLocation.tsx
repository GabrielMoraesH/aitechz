import { StoreMap } from "@/components/ui/StoreMap/StoreMap";
import { ButtonLink } from "@/components/ui/Button/Button";
import { Container } from "@/components/ui/Container/Container";
import { createWhatsAppUrl, formatStoreAddress, whatsappMessages, type PublicStoreSettings } from "@/lib/storeSettings";
import styles from "./StoreLocation.module.css";
export function StoreLocation({ settings }: { settings: PublicStoreSettings }) { const address = formatStoreAddress(settings); return <section id="contato" className={styles.section}><Container className={styles.layout}><div><p className={styles.eyebrow}>Nossa loja</p><h2 className={styles.title}>Estamos esperando por você!</h2><p className={styles.description}>Visite nossa loja física em {settings.city}.</p><address className={styles.address}>{address.map((line) => <span key={line}>{line}<br /></span>)}</address><div className={styles.actions}>{settings.mapsUrl && <ButtonLink href={settings.mapsUrl} target="_blank" rel="noopener noreferrer" aria-label="Abrir localização da Aitechz no Google Maps" variant="light">Como chegar</ButtonLink>}<ButtonLink href={createWhatsAppUrl(settings.whatsapp, whatsappMessages.general)} target="_blank" rel="noopener noreferrer" aria-label="Falar com a Aitechz pelo WhatsApp" variant="light">Falar conosco</ButtonLink></div></div><StoreMap settings={settings} /></Container></section>; }
